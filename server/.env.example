# Copy to .env and adjust for your machine.
PORT=3001

DB_HOST=127.0.0.1
DB_USER=gasoleo
DB_PASSWORD=your_password
DB_NAME=gasoleo

# Optional: restrict CORS (default in dev allows any origin via *)
# CORS_ORIGIN=http://localhost:8081
